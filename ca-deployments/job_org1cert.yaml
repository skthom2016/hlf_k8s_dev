apiVersion: batch/v1
kind: Job
metadata:
  name: create-org1-certs
spec:
  parallelism: 1
  completions: 1
  template:
    metadata:
      name: create-certs
    spec:
      volumes:
        - name: org1-ca
          persistentVolumeClaim:
            claimName: org1-pvc
        - name: redis-storage
          persistentVolumeClaim:
            claimName: mypvc
      containers:
        - name: create-certs
          image: hyperledger/fabric-ca:1.4.4
          resources:
            requests:
              memory: "300Mi"
              cpu: "300m"
            limits:
              memory: "500Mi"
              cpu: "350m"
          volumeMounts:
            - mountPath: /organizations
              name: org1-ca
            - mountPath: /scripts
              name: redis-storage
          command:
            - /bin/sh
            - -c
            - |
              /scripts/org1-certs.sh 
      restartPolicy: Never
